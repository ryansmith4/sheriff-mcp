# Project version is derived from git tags via axion-release-plugin
# See build.gradle.kts scmVersion configuration
# Use ./gradlew release to tag a release
# Use ./gradlew currentVersion to check the current version

org.gradle.configuration-cache=true
